{
  "name": "quantum-wormhole-wasm-rs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:wasm": "npx wasm-pack build --target web crate",
    "build": "pnpm build:wasm && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@material/web": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-top-level-await": "^1.4.4",
    "vite-plugin-wasm": "^3.4.1"
  }
}
